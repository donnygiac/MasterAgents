# ideas/

Storico dei design di schema, un file per progetto/argomento
(`<project-name>.md`). Viene popolato dalla skill `designing-mysql-tables`
durante il lavoro — non eliminare: è la memoria delle decisioni prese.
